import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  scrollViewContainer: {
    marginBottom: 1,
  },
  keyPadContainer: {
    marginTop: 15,
  },
})

export default styles
