import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  formContainer: {
    marginTop: 33,
    marginBottom: 50,
  },
  scrollViewContainer: {
    marginBottom: 100,
  },
  backButtonContainer: {
    paddingBottom: 10,
  },
})

export default styles
