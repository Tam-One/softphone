import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  formContainer: {
    marginTop: 16,
    marginBottom: 80,
  },
  scrollViewContainer: {
    marginBottom: 1,
  },
  backButtonContainer: {
    paddingBottom: 10,
  },
  contactContainer: {
    marginVertical: 16,
  },
})

export default styles
