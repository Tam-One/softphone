import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  CustomGradient: {
    zIndex: 99,
    position: 'absolute',
    width: '100%',
    height: '100%',
    flex: 1,
    resizeMode: 'cover',
  },
})

export default styles
